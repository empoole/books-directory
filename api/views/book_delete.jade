extends layout

block content
  h1 #{title}: #{book.title}

    p Do you really want to delete this book?

    form(method='post' action='')
      div.form-group
        input(type='hidden', id='book-id', class='form-control', name='book_id', required='true', value=book._id )

      button(type='submit' class='btn.btn-primary') Delete